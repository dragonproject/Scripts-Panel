function createProgressBarPanel(maxValue, pBarWidth, message){	var pBar = new Window("window", "Progress");	pBar.msg = pBar.add("statictext", [12,12,pBarWidth,32], message);	pBar.progressBar = pBar.add("progressbar", [12, 32, pBarWidth, 54], 0, maxValue);	return pBar;}var pbarWidth = 400;	// プログレスバーの幅var max = 300;	// 最大値var pbarPanel = createProgressBarPanel(max, pbarWidth, "処理中...");pbarPanel.show();	// プログレスバーを表示// for(var counter = 0; counter <max; counter ++){	pbarPanel.progressBar.value = counter;	// プログレスバーの割合を設定する	process();	// ページを追加する関数を呼び出す}pbarPanel.close();	// プログレスバーを消す// 実際に処理する内容を関数にしておいて呼び出すfunction process(){	var pageObj = app.activeDocument.pages.add();	// ページを追加する}
