(function sample0001(){	var pageNo = 0;	// 1ページ目	var textframeNo = 0;	// 最前面のテキストフレーム	var paraNo = 0;	// 最初の段落	var tfObj = app.activeDocument.pages[pageNo].textFrames[textframeNo];	// 処理対象のテキストフレームを指定	for(var i=0; i<tfObj.paragraphs.length; i++)	// 全ての段落を処理する	{		if (tfObj.paragraphs[i].characters[0].contents == "■")		{			tfObj.paragraphs[i].justification = Justification.centerAlign;	// 中央揃えにする		}	}})();
