(function(){	var Color01 = app.activeDocument.colors.item("黄色");	if (Color01 == null)	{		var C = 0;	// シアン		var M = 0;	// マゼンタ		var Y = 100;	// イエロー		var K = 0;	// ブラック		var colorOption = {colorValue:[C,M,Y,K],model:ColorModel.process,space:ColorSpace.cmyk,name:"黄色"}		Color01 = app.activeDocument.colors.add(colorOption);	}	// サンプルとなる表を生成	var tfObj = app.activeDocument.textFrames.add();	tfObj.visibleBounds = ["25mm","30mm","100mm","200mm"];	var tableObj = tfObj.tables.add();	tableObj.bodyRowCount = 4;	// 4行	tableObj.columnCount = 4;	// 4列	var data = [];	for (var i=0; i<16; i++)	{		data[i] = ""+Math.floor(Math.random()*100);	}	tableObj.contents = data;	// 背景色を設定	tableObj.rows[2].cells[1].fillColor = Color01;})();
