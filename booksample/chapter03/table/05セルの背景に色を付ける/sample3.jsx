(function(){	var colorObj = new Array();	colorObj[0] = app.activeDocument.colors.item("黄色");	if (colorObj[0] == null)	{		colorObj[0] = app.activeDocument.colors.add({colorValue:[0,0,100,0],model:ColorModel.process,space:ColorSpace.cmyk,name:"黄色"});	}	colorObj[1] = app.activeDocument.colors.item("水色");	if (colorObj[1] == null)	{		colorObj[1] = app.activeDocument.colors.add({colorValue:[100,0,0,0],model:ColorModel.process,space:ColorSpace.cmyk,name:"水色"});	}	colorObj[2] = app.activeDocument.colors.item("緑色");	if (colorObj[2] == null)	{		colorObj[2] = app.activeDocument.colors.add({colorValue:[100,0,100,0],model:ColorModel.process,space:ColorSpace.cmyk,name:"緑色"});	}	// サンプルとなる表を生成	var tfObj = app.activeDocument.textFrames.add();	tfObj.visibleBounds = ["25mm","30mm","100mm","200mm"];	var tableObj = tfObj.tables.add();	tableObj.bodyRowCount = 9;	// 9行	tableObj.columnCount = 4;	// 4列	var data = [];	for (var i=0; i<36; i++)	{		data[i] = ""+Math.floor(Math.random()*100);	}	tableObj.contents = data;	// 背景色を設定	for (i=0; i<tableObj.bodyRowCount; i++)	// tableObj.bodyRowCount分処理する	{		var n = i % colorObj.length;	// colorObj配列で指定された色の数で繰り返す		tableObj.rows[i].fillColor = colorObj[n];	}})();
