(function sample0001(){	colorObj = app.activeDocument.colors.item("赤");	if (colorObj == null)	{		var C = 10;	// シアン		var M = 100;	// マゼンタ		var Y = 50;	// イエロー		var K = 10;	// ブラック		var colorOption = {colorValue:[C, M, Y, K],model:ColorModel.process,space:ColorSpace.cmyk,name:"赤"};		colorObj = app.activeDocument.colors.add(colorOption);	}	var tfObj = app.activeDocument.pages[0].textFrames[0];	for (var i=0; i<tfObj.paragraphs.length; i++)	{		if (tfObj.paragraphs[i].characters[0].contents == "■")		{			tfObj.paragraphs[i].characters[0].fillColor = colorObj;		}	}})();
