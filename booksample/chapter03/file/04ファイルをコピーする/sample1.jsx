(function sample0001(){	var sourceFile = File.openDialog("コピー元のファイルを指定してください");	if (sourceFile)	{		var folderObj = Folder.selectDialog("コピー先のフォルダを選択してください");		if (folderObj)		{			var copyFile = new File(folderObj.fsName + "/" + sourceFile.name);			var flag = (new File(sourceFile)).copy(copyFile);			if (flag == false)			{				alert("正しくファイルがコピーできませんでした");			}		}	}})();
