(function sample0001(){	var fileObj = File.saveDialog("保存ファイル名を入れて下さい");	if (fileObj)	{		var flag = fileObj.open("w");	// ファイルを書き込みモードで開く		if (flag == true)		{			var text1 = "ファイルに文字を書き出します";			var text2 = "Sample text";			var text3 = "InDesignスクリプトサンプル";			fileObj.write(text1);	// ファイルを改行なしで書き出す			fileObj.writeln(text2);	// ファイルを改行コード付きで書き出す			fileObj.writeln(text3);	// ファイルを改行コード付きで書き出す			fileObj.close();	// ファイルを閉じる		}else{			alert("書き込むファイルを開けません");		}	}})();
