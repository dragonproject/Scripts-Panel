(function(){	var cutString  = "。";	// 。（末尾の文字）	var selObj = app.selection;	for (var i=0; i<selObj.length; i++){		if (selObj[i].overflows == true) {			alert("テキストがオーバーフローしており全て表示されていません");			app.select(selObj[i]);			return;		}		var txt = selObj[i].paragraphs[-1].contents;		var textRange = selObj[i].paragraphs[0].insertionPoints.itemByRange(txt.length,txt.length-1);		if (textRange.contents == cutString){			textRange.remove();		}	}})();
