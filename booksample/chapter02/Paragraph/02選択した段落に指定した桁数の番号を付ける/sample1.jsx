(function(){	var keta = eval(prompt("桁数を入れてください",3));	var prefix = "";	for (var i=0; i<keta; i++){		prefix = prefix + "0";	// 0でなく空白にすることも可能	}	var selObj = app.activeDocument.selection;	var paraObj = selObj[0].paragraphs;	var count = paraObj.length;	for (i=paraObj.length-1; i>=0; i--){		var str = prefix + count;		var n = str.substr(str.length - keta, keta);		paraObj[i].insertionPoints[0].contents = n + " : ";		count--;	}})();
