(function(){	var tag = prompt("置換対象のタグ（文字）を入れてください","冷凍マーク");	if (tag == null) return;	// キャンセルされたら処理しない	var imgPath = File.openDialog("画像ファイルを指定してください");	if (imgPath == null) return;	// キャンセルされたら処理しない	var selObj = app.selection;	for (var i=0; i<selObj.length; i++){		var p = 0;		var sp = 0;	// 検索開始位置		while (p > -1){			p = selObj[i].contents.indexOf(tag, sp);			if (p > -1) {				app.select(selObj[i].insertionPoints.itemByRange(p, p+tag.length));				var flag = confirm("置換しますか？");				if (flag == true){					selObj[i].insertionPoints.itemByRange(p, p+tag.length).place(imgPath);				}				sp = p + tag.length; 			}		}	}})();
