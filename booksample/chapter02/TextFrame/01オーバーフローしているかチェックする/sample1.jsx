(function(){	var docObj = app.activeDocument;    setColor = [0, 100, 100, 0];    Color1 = docObj.colors.add({model:ColorModel.process,space:ColorSpace.cmyk,colorValue:setColor});	for (var i=0; i<docObj.pages.length; i++)	{        var tfObj = docObj.pages[i].textFrames;        for (var j=0; j<tfObj.length; j++)		{			if (tfObj[j].overflows == true)			{                alert((i+1)+"ページ目の"+(j+1)+"番目のテキストフィールドにオーバーフローしたテキストフレームがあります");                alert (Color1);                // tfObj[j].fillColor(Color1);				// return;			}		}	}})();
