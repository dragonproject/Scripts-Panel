function layoutTextFile(filePath){	var fileObj = new File(filePath);	// 関数に渡されたパスをパラメータとして指定	var flag = fileObj.open("r");	if (flag == true)	// ファイルが正しく開けた場合のみ処理する	{		var docObj = app.activeDocument;		var tfObj = docObj.pages[0].textFrames.add();	// テキストフレームを追加		tfObj.visibleBounds = ["0mm","0mm","297mm","210mm"];	// A4サイズのテキストフレームを作成		tfObj.contents = fileObj.read();		fileObj.close();	}}// 実際に関数を呼び出すlayoutTextFile("~/IDSampleFolder/sample.txt");
